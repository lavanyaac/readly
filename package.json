{
  "name": "readly",
  "version": "1.0.0",
  "description": "Reddit Reader",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "repository": {
    "type": "git"
  },
  "author": "Lavanya",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
